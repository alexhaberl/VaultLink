files.example.com {
    reverse_proxy unix//run/vaultlink-proxy/http.sock
    request_body {
        # The 1 GB file payload needs room for multipart boundaries and fields.
        max_size 1002MB
    }
}
