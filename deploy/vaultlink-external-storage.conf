# Install as /etc/systemd/system/vaultlink.service.d/external-storage.conf.
# Keep this path at the validated mount base that contains both
# storage.root_mount_path and storage.internal_directory.
[Unit]
RequiresMountsFor=/mnt/storage
After=remote-fs.target
