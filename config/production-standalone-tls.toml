[server]
mode = "standalone_tls"
listen_address = "0.0.0.0:443"
public_base_url = "https://files.example.com"
production_mode = true

[storage]
root_mount_path = "/mnt/storage"
data_directory = "/var/lib/vaultlink"
max_upload_size = 1073741824
blocked_extensions = ["exe", "dll", "com", "bat", "cmd", "ps1", "sh"]

[security]
session_hours = 12
login_attempts = 5
login_window_seconds = 300
secure_cookie = true

[reverse_proxy]
enabled = false
trusted_proxies = []
trust_x_forwarded_headers = false

[tls]
enabled = true
cert_file = "/etc/vaultlink/tls/fullchain.pem"
key_file = "/etc/vaultlink/tls/privkey.pem"
hsts_enabled = true
redirect_http_to_https = false
reload_on_cert_change = false

[logging]
level = "info"
audit_log_path = "/var/log/vaultlink/audit.log"

