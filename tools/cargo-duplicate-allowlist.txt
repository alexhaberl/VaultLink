# Stable WebAuthn/passkey dependencies still span the pre-1.0 and current utility
# stacks. Re-evaluate when webauthn_rp and the crypto stack converge.
const-oid
getrandom
rand
rand_core

# Argon2/password hashing, TOTP, XChaCha and WebAuthn currently pull compatible,
# but distinct crypto generations. Re-evaluate after their stable upgrades.
block-buffer
cpufeatures
crypto-common
digest
hmac
rand-chacha
sha2

# Stable dependency branches currently use both thiserror major versions. Remove
# these entries as soon as all direct parents have converged on one major.
thiserror
thiserror-impl

# Proc-macro dependencies temporarily span syn 2 and 3: futures-macro,
# serde_derive, and thiserror-impl 2.x have moved to syn 3 while remaining
# macro crates still require syn 2. Remove once the proc-macro graph converges.
syn

# asn1-rs-derive 0.6 still requires synstructure 0.13, while yoke-derive 0.8.3
# and zerofrom-derive 0.1.8 require 0.14. Remove once their macro dependencies
# converge on one version.
synstructure

# The ACME/TLS dependency graph still spans two webpki-roots generations.
# Re-evaluate after the next stable rustls-acme/rustls upgrade.
webpki-roots

# `--target all` intentionally exposes platform support generations that are not
# compiled together on one host. Re-evaluate after their parent crates converge.
hashbrown
r-efi
